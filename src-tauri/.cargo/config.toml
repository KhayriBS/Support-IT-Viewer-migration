[build]
# Workaround for Windows security/AV blocking executable creation under protected folders (e.g. Desktop).
# Redirect build artifacts to a user-writable location.
target-dir = "C:\\cargo-target\\lumiere-tech-it"
